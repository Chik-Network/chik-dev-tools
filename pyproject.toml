[build-system]
requires = [
    "setuptools>=80",
    "setuptools_scm>=8",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "chik_dev_tools"
description = "Chik development commands"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "CNI", email = "hello@chiknetwork.com" },
]
license = "Apache-2.0"
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Topic :: Security :: Cryptography",
]
dynamic = ["version"]
dependencies = [
    "packaging",
    "pytest",
    "pytest-asyncio",
    "pytimeparse",
    "anyio",
    "chik-blockchain==2.7.3",
]

[project.optional-dependencies]
dev = [
    "anyio",
    "mypy",
    "ruff==0.14.6",
    "types-aiofiles",
    "types-click",
    "types-cryptography",
    "types-setuptools",
    "types-pyyaml",
    "pre-commit",
]

[project.scripts]
cdv = "cdv.cmds.cli:main"

[project.urls]
"Bug Reports" = "https://github.com/Chik-Network/chik-dev-tools"
Source = "https://github.com/Chik-Network/chik-dev-tools"
Homepage = "https://github.com/Chik-Network"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
exclude = ["tests*"]

[tool.setuptools.package-data]
"*" = ["*.klvm", "*.klvm.hex", "*.clib", "*.clsp", "*.clsp.hex"]

[tool.setuptools_scm]
